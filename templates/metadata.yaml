# 实验元数据模板
# 由 labshelf.py new 自动填充

id: "{id}"
created: "{created}"
updated: "{updated}"
purpose: "{purpose}"
tags: {tags}
status: "{status}"

data: {{}}                  # 数据注册表（逻辑名 → 文件 + 环境/溯源）

outputs: {{}}               # 输出注册表（输出名 → 文件/脚本/输入）
